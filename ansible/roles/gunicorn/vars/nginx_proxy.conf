server {
    listen 80;
    server_name _;
    location / {
        include proxy_params;
        proxy_pass http://unix:/home/ruser_placeholder/rapp_folder/rapp_name;
        proxy_read_timeout 180;
        proxy_connect_timeout 180;
        proxy_send_timeout 180;
        send_timeout 180;
    }
}